(()=>{const style=document.createElement('link');style.rel='stylesheet';style.href='/ui-fixes.css?v=4';document.head.appendChild(style);const $=id=>document.getElementById(id);const money=v=>Number(v||0).toLocaleString('pt-BR',{style:'currency',currency:'BRL'});const showUnified=id=>{document.querySelectorAll('.view').forEach(v=>v.classList.toggle('on',v.id===id));document.querySelectorAll('[data-view]').forEach(b=>b.classList.toggle('on',b.dataset.view===id));document.querySelector('.main')?.scrollTo({top:0,behavior:'smooth'})};document.querySelectorAll('[data-view]').forEach(b=>b.addEventListener('click',e=>{const id=e.currentTarget.dataset.view;if(document.getElementById(id))showUnified(id)}));const flow=['Empreendimento','Documentos','Compatibilização','Ocorrências','Revisões','Impactos','Orçamento','Planejamento','Mudanças','Relatório','Intelligence'];function renderFlow(){const root=$('unifiedFlow');if(!root)return;root.innerHTML=flow.map((name,i)=>`<div class="flow-step ${i<4?'done':''}"><i>${String(i+1).padStart(2,'0')}</i><span>${name}</span></div>`).join('')}function opIssueNode(i){const sev=i.severity||'media';return `<article class="issue ${sev==='critica'?'critical':sev==='alta'?'high':''}"><header><code>${i.code||'VLT'}</code><b>${sev.toUpperCase()}</b></header><h4>${i.title}</h4><p>${i.description}</p><small>${i.location||'Local não informado'} · ${i.assignee||'Sem responsável'} · ${String(i.status||'identificada').replaceAll('_',' ')}</small></article>`}async function loadOperational(){if(!window.S?.projectId)return;try{const d=await window.api(`/api/projects/${window.S.projectId}/operational/dashboard`);$('opOpen').textContent=d.openIssues||0;$('opCritical').textContent=`${d.criticalIssues||0} críticas`;$('opCost').textContent=money(d.estimatedCost);$('opDays').textContent=`${d.estimatedDays||0} dias`;$('issueOpen').textContent=d.openIssues||0;$('issueCritical').textContent=d.criticalIssues||0;$('issueCost').textContent=money(d.estimatedCost);$('issueDays').textContent=`${d.estimatedDays||0} dias`;const sev=d.bySeverity||{};$('riskCritical').textContent=sev.critica||0;$('riskHigh').textContent=sev.alta||0;$('riskMedium').textContent=sev.media||0;$('riskLow').textContent=sev.baixa||0;const html=(d.recent||[]).map(opIssueNode).join('')||'<div class="empty">Nenhuma ocorrência operacional registrada.</div>';$('overviewIssues').innerHTML=html;$('issueList').innerHTML=html}catch(e){$('overviewIssues').innerHTML='<div class="empty">O núcleo operacional ainda não respondeu neste ambiente.</div>'}}function syncBaseMetrics(){const files=window.S?.files||[];$('opFiles').textContent=files.length;$('opDisciplines').textContent=`${new Set(files.map(f=>f.discipline_code)).size} disciplinas`;const a=window.S?.analysis;const readiness=a?.readiness||0;$('overviewReadiness').textContent=`${readiness}%`;$('overviewCompat').textContent=`${readiness}%`;$('overviewCompatBar').style.width=`${readiness}%`;$('overviewGate').textContent=a?.gate||'RODADA NÃO EXECUTADA';$('overviewCompatText').textContent=a?`Rodada com ${a.files} arquivos, ${a.disciplines?.length||0} disciplinas e ${a.interfacePackages?.length||0} interfaces.`:'Execute uma rodada para avaliar a base.'}const openIssue=()=>{$('issueModal')?.classList.add('open')};$('createIssue')?.addEventListener('click',openIssue);$('quickIssue')?.addEventListener('click',openIssue);$('cancelIssue')?.addEventListener('click',()=>{$('issueModal').classList.remove('open')});$('issueForm')?.addEventListener('submit',async e=>{e.preventDefault();if(!window.S?.projectId)return;const payload={title:$('iTitle').value,description:$('iDescription').value,issueType:$('iType').value,severity:$('iSeverity').value,location:$('iLocation').value,disciplines:$('iDisciplines').value.split(',').map(v=>v.trim()).filter(Boolean),assignee:$('iAssignee').value};try{await window.api(`/api/projects/${window.S.projectId}/operational/issues`,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(payload)});e.target.reset();$('issueModal').classList.remove('open');await loadOperational();showUnified('issues')}catch(err){window.fail?.(err.message)}});const oldRender=window.render;window.render=function(){oldRender?.();syncBaseMetrics();loadOperational()};const oldLoadProject=window.loadProject;window.loadProject=async function(){await oldLoadProject?.();syncBaseMetrics();await loadOperational()};renderFlow();setTimeout(()=>{syncBaseMetrics();loadOperational()},900)})();