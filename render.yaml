services:
  - type: web
    name: vaelith-platform
    runtime: docker
    plan: starter
    healthCheckPath: /api/health
    envVars:
      - key: PORT
        value: 8080
      - key: COOKIE_SECURE
        value: 1
